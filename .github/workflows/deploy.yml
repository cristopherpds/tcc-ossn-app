name: Build and Push Docker Image

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.repository_owner }}
        password: ${{ secrets.GHCR }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        tags: ghcr.io/${{ github.repository_owner }}/tcc-ossn-app:${{ github.ref_name }}
        labels: |
          org.opencontainers.image.source=${{ github.repository }}

    - name: Checkout tcc-ops repository
      uses: actions/checkout@v2
      with:
        repository: cristopherpds/tcc-ops
        token: ${{ secrets.PAT }}  # Personal Access Token (PAT)
        path: tcc-ops
        ref: deploy/app  # We work with the deploy/app branch

    # 6. Update the image tag in deployment.yml
    - name: Update image tag in deployment.yml
      run: |
        TAG=${GITHUB_REF##*/}
        VERSION=${TAG#v}
        sed -i "s|image: ghcr.io/${{ github.repository_owner }}/tcc-ossn-app:.*|image: ghcr.io/${{ github.repository_owner }}/tcc-ossn-app:$VERSION|" tcc-ops/k8s/deployment.yml

    # 7. Commit and push changes
    - name: Commit and push changes to deploy/app
      run: |
        cd tcc-ops
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"

        git add k8s/deployment.yml
        git commit -m "chore(deploy): release $VERSION"
        git push origin deploy/app
